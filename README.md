# 🚀 Dynamic EV Pricing Fullstack App

Production-ready fullstack app for EV charging dynamic pricing, station discovery, trip planning, and user dashboard.

## Tech Stack
Frontend: Next.js, React, Tailwind CSS  
Backend: Node.js, Express, MongoDB, JWT  
Database: MongoDB  

## Setup

Install dependencies:
cd backend && npm install  
cd frontend && npm install  

## Run Project

Backend:
cd backend && npm start  

Frontend:
cd frontend && npm run dev  

## URLs
Frontend: http://localhost:3000  
Backend: http://localhost:5000  

## Environment

Backend .env:
PORT=5000  
MONGO_URI=your_mongo_url  
JWT_SECRET=your_secret  

Frontend .env.local:
NEXT_PUBLIC_API_URL=http://localhost:5000  

## Git Commands
git add .  
git commit -m "clean production ready project"  
git push origin main  

## Notes
- Do not upload node_modules  
- Do not upload .env  
- Do not upload .next / build files  
